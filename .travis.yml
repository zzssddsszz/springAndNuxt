language: java
jdk:
  - openjdk11

branches:
  only:
    - CI/CD

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_install:
  - chmod +x gradlew

notifications:
  email:
    recipients:
      modoodesigner@gmail.com